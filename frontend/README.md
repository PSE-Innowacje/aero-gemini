# Frontend Development

Use the shared Cursor + Compose workflow documented in:

- `backend/docs/cursor-compose-dev.md`

Quick start:

```bash
cd /workspace/infra
docker compose up --build backend frontend
```

Frontend runs at `http://localhost:8080`.
