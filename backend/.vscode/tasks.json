{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compose: up backend+frontend",
      "type": "shell",
      "command": "docker compose up --build backend frontend",
      "options": {
        "cwd": "${workspaceFolder}/../infra"
      },
      "problemMatcher": []
    },
    {
      "label": "compose: logs frontend",
      "type": "shell",
      "command": "docker compose logs -f frontend",
      "options": {
        "cwd": "${workspaceFolder}/../infra"
      },
      "problemMatcher": []
    },
    {
      "label": "compose: down",
      "type": "shell",
      "command": "docker compose down",
      "options": {
        "cwd": "${workspaceFolder}/../infra"
      },
      "problemMatcher": []
    }
  ]
}
